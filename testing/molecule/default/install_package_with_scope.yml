---
- name: Install package with scope test
  hosts: all
  become: false

  tasks:
    - name: Install package
      raw: "nupac install make -a"
      args:
        executable: "{{ nu }}"

    - name: Assert package file was created and added to scope
      block:

        - name: Stat make.nu
          stat:
            path: "{{ nushell_dir }}/scripts/make.nu"
          register: package

        - name: Get nushell scope
          raw: "$nu.scope.commands|get command|to json"
          args:
            executable: "{{ nu }}"
          register: scope

        - name: Run assertions
          assert:
            that:
              - package.stat.exists
              - '"make" in {{ scope.stdout | from_json }}'

